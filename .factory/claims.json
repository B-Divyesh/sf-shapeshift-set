[
  {
    "id": "daily-end",
    "claim": "Each daily board has five creatures and reaches a scored end screen.",
    "where": "game, README",
    "test": "npm test -- --grep @claim:daily-end",
    "sandbox": "fresh browser context at /demo; complete the five-piece sample run"
  },
  {
    "id": "board-size",
    "claim": "Each daily board is 6 by 6.",
    "where": "opening label, game board, README",
    "test": "npm test -- --grep @claim:board-size",
    "sandbox": "fresh browser context at /demo; inspect the rendered sample board for six rows, six columns, and 36 targets"
  },
  {
    "id": "unique-perfect",
    "claim": "Each board has one perfect five-change order, and consecutive daily answers differ.",
    "where": "result screen, README",
    "test": "npm test -- --grep @claim:unique-perfect",
    "sandbox": "enumerate all 120 placement orders for 48 UTC dates and reject consecutive repeated answers"
  },
  {
    "id": "restart",
    "claim": "Play this board again resets all pieces and the score.",
    "where": "result screen",
    "test": "npm test -- --grep @claim:restart",
    "sandbox": "finish /demo, replay, and assert zero score and an empty changed-neighbor trail"
  },
  {
    "id": "local-progress",
    "claim": "Progress stays in this browser and survives reloads.",
    "where": "landing facts, privacy, README",
    "test": "npm test -- --grep @claim:local-progress",
    "sandbox": "fresh browser storage; place one real piece, reload, and inspect state"
  },
  {
    "id": "demo-isolation",
    "claim": "Demo uses memory only and never reads or changes real progress.",
    "where": "demo banner, privacy",
    "test": "npm test -- --grep @claim:demo-isolation",
    "sandbox": "seed valid sample-date and current-day runs, a cookie, and an OPFS file; record real-progress reads; play and reset /demo; inspect localStorage, cookies, IndexedDB, and OPFS; then return to the separately stored current-day run"
  },
  {
    "id": "offline-reload",
    "claim": "The game can reopen offline after the first visit.",
    "where": "scope and privacy section, README",
    "test": "npm test -- --grep @claim:offline-reload",
    "sandbox": "dedicated browser context at /demo; wait for service worker, go offline, and reload"
  },
  {
    "id": "piece-settle-duration",
    "claim": "Placed creatures settle in 220 milliseconds when motion is enabled.",
    "where": "README",
    "test": "npm test -- --grep @claim:piece-settle-duration",
    "sandbox": "fresh /demo context; place Mote and inspect its rendered animation duration"
  },
  {
    "id": "frame-rate",
    "claim": "The game loop has a 60 frames-per-second timing target.",
    "where": "README",
    "test": "npm test -- --grep @claim:frame-rate",
    "sandbox": "fresh /demo context; measure fixed-timestep ticks over one second"
  },
  {
    "id": "keyboard-controls",
    "claim": "Keyboard controls can select, rotate, flip, move, and place creatures.",
    "where": "README",
    "test": "npm test -- --grep @claim:keyboard-controls",
    "sandbox": "fresh /demo context using number, Q/E, F, Arrow, and Space keys"
  },
  {
    "id": "all-inputs",
    "claim": "Pointer and touch controls can select, turn, and place creatures.",
    "where": "README",
    "test": "npm test -- --grep @claim:all-inputs",
    "sandbox": "fresh desktop and touch /demo contexts using click and tap controls"
  },
  {
    "id": "undo-last-piece",
    "claim": "Undo last piece returns the latest creature to the tray and removes its score.",
    "where": "game controls",
    "test": "npm test -- --grep @claim:undo-last-piece",
    "sandbox": "fresh /demo context; place Mote, undo, and inspect score, tray, and changed-neighbor score"
  },
  {
    "id": "copy-result",
    "claim": "Copy result puts the completed score, result, and Board ID on the clipboard.",
    "where": "result screen",
    "test": "npm test -- --grep @claim:copy-result",
    "sandbox": "fresh /demo context with clipboard permission; complete a perfect board and read copied text"
  },
  {
    "id": "session-length",
    "claim": "A session is one five-placement board.",
    "where": "README",
    "test": "npm test -- --grep @claim:session-length",
    "sandbox": "fresh /demo context; verify no result after four placements and a result after five"
  },
  {
    "id": "persistence-recovery",
    "claim": "When browser storage rejects a write, the game says the current run will not survive reload.",
    "where": "game status message",
    "test": "npm test -- --grep @claim:persistence-recovery",
    "sandbox": "fresh real-play context with Storage.setItem throwing QuotaExceededError; place Mote and reload"
  },
  {
    "id": "same-origin",
    "claim": "The game loads no third-party runtime resources.",
    "where": "privacy, README",
    "test": "npm test -- --grep @claim:same-origin",
    "sandbox": "record every request during a /demo move and assert the product origin"
  },
  {
    "id": "no-analytics",
    "claim": "The game sends no analytics requests or event payloads.",
    "where": "privacy, README",
    "test": "npm test -- --grep @claim:no-analytics",
    "sandbox": "record requests during a complete demo run and replay; reject analytics endpoints and event payloads"
  },
  {
    "id": "no-leaderboard",
    "claim": "The game has no public leaderboard.",
    "where": "scope and privacy section, README",
    "test": "npm test -- --grep @claim:no-leaderboard",
    "sandbox": "finish a demo board and inspect all visible text and controls for leaderboard output"
  },
  {
    "id": "mutation-scoring",
    "claim": "An arrow scores only when its target creature is already on the board.",
    "where": "game rule, How to play, README",
    "test": "npm test -- --grep @claim:mutation-scoring",
    "sandbox": "place a dependent before and after its target and assert the score and itemized changed-neighbor trail"
  },
  {
    "id": "score-tiers",
    "claim": "Results show Try again (0–2), Close (3–4), or Perfect (5), with one itemized result per creature.",
    "where": "result screen, README",
    "test": "npm test -- --grep @claim:score-tiers",
    "sandbox": "finish deterministic demo runs at scores 1, 3, and 5 and inspect tier labels and all five trail items"
  },
  {
    "id": "utc-daily",
    "claim": "A new shared board appears each UTC day.",
    "where": "landing facts, README",
    "test": "npm test -- --grep @claim:utc-daily",
    "sandbox": "construct two same-date boards and one next-date board, then compare seeds and geometry"
  },
  {
    "id": "free-no-upsells",
    "claim": "The complete game is free and has no accounts, ads, boosters, or purchase controls.",
    "where": "landing facts, scope and privacy section, README",
    "test": "npm test -- --grep @claim:free-no-upsells",
    "sandbox": "finish /demo and inspect the complete playable flow for account or payment controls"
  }
]
